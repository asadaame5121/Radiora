- 現行実装はリポジトリ直下にある。旧Wails版は`spinning-granule-next`リポジトリを参照する
- 実装完了・change確定・push前に`deno task verify`を実行する
- 検証失敗中は完了扱いにしない
- Jujutsuを使用し、通常のGitコミット手順を前提にしない
- 日本語で応答する
- 生成物や診断ファイルをコミットしない

## マルチエージェント運用

- 親エージェントはSol・mediumを使用し、ユーザーとの会話、要件整理、計画、受け入れ条件、サブエージェントへの指示、変更レビュー、統合判断、最終報告に責任を持つ
- スコープと受け入れ条件が明確な一般実装は、Terra・mediumのサブエージェントへ委譲する
  - 小さな機能追加
  - 機械的な修正やリファクタ
  - テスト追加
  - 対象範囲のlint・test
- 次の難所はSol・mediumのサブエージェントへ委譲する
  - 原因不明の不具合
  - 複数レイヤーをまたぐ変更
  - 状態管理、永続化、並行処理
  - 設計上の選択肢が複数ある箇所
- Terra担当が次のいずれかに該当した場合、親エージェントが作業を回収し、Sol・mediumへ昇格する
  1. 1回の実装修正後も同じ検証が失敗する
  2. 仕様または既存コードから不変条件を確定できない
  3. 変更がUI、状態管理、永続化など複数領域にまたがる
  4. データ破損または後方互換性のリスクがある
  5. 複数の実装案から設計判断が必要になる
- Sol・high以上は、mediumで解決できない難所、セキュリティ境界、またはデータ破損リスクが高い変更に限定する
- サブエージェントには会話履歴を必要以上に渡さず、達成する結果、編集可能範囲、壊してはいけない条件、実行する検証、停止して親へ返す条件を明示する
- 共有ワークツリーでは同じファイルを複数エージェントに並行編集させない。原則として「Terraが実装 → 親が評価 → 必要ならSolへ昇格」の順で進める
- `deno task verify`の最終実行と、change確定・push可否の判断は親エージェントが行う
