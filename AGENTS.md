- 現行実装はリポジトリ直下にある。旧Wails版は`spinning-granule-next`リポジトリを参照する
- 実装完了・change確定・push前に`deno task verify`を実行する
- 検証失敗中は完了扱いにしない
- Jujutsuを使用し、通常のGitコミット手順を前提にしない
- 日本語で応答する
- 生成物や診断ファイルをコミットしない

## マルチエージェント運用

### 親エージェント（Sol・medium）

- ユーザーとの会話、要件整理、計画、受け入れ条件、テスト計画、担当への指示、変更レビュー、統合判断、最終報告を担う。
- Cloud成果は親がレビューして採否を決め、自動統合しない。`deno task verify` の最終実行、change確定、push可否の判断も親だけが行う。

### ローカル子エージェント（Terra・medium を基本）

- スコープと受け入れ条件が明確な小機能、機械的修正・リファクタ、テスト追加を、限定された編集範囲で実装する。対象範囲の lint・test 実行は許容するが、全体の最終ゲートは親に委ねる。
- 指示には、達成結果、編集可能範囲、不変条件、実行する検証、停止して親へ返す条件を明記する。新規ファイルまたは排他的所有範囲を優先する。
- 次のいずれかに該当した場合は親が作業を回収し、Sol・mediumへ昇格する。
  1. 1回の実装修正後も同じ検証が失敗する
  2. 仕様または既存コードから不変条件を確定できない
  3. 変更がUI、状態管理、永続化など複数領域にまたがる
  4. データ破損または後方互換性のリスクがある
  5. 複数の実装案から設計判断が必要になる
- 原因不明の不具合、複数レイヤーの変更、状態管理・永続化・並行処理は、最初からSol・mediumへ委譲してよい。Sol・high以上はmediumで解決できない難所、セキュリティ境界、または高いデータ破損リスクに限定する。

### Codex Cloud（先行調査を基本）

- デフォルトは読み取り専用とし、最終回答で調査結果を報告する。コードベース・issue調査、高密度テスト設計、将来のリファクタリング候補調査など、他担当と競合しない先行作業を担う。
- 実装を依頼する場合は base revision、編集可能範囲、禁止ファイル、不変条件、検証、停止条件を明示する。新規ファイルまたは排他的所有範囲だけを優先し、共有ファイルへの変更は親の明示的判断がある場合に限る。
- `src/ui/App.svelte`、`src/domain/models.ts`、`src/shared/bindings.ts`、`src/shared/ui_vocabulary.ts`、`src/storage/graph_store.ts`、migration登録、lockfileなどのhot spotは並列編集しない。

### 共通ルール

- 親は作業開始前に `jj status` と `jj diff` で作業ツリーを確認し、担当ごとの所有範囲を決める。同一ファイルを複数担当に並行編集させない。
- GitHubのissue / PRの確認や操作は`gh` CLIを優先する。
- 生成物や診断ファイルはコミットしない。
